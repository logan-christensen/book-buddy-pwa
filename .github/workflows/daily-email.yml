name: Daily Story Email

on:
  schedule:
    - cron: '0 14 * * *'  # 9am EST / 2pm UTC
  workflow_dispatch:       # manual trigger for testing

jobs:
  send-email:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Send daily questions email
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          DATA_GITHUB_TOKEN: ${{ secrets.DATA_GITHUB_TOKEN }}
          DATA_GITHUB_OWNER: logan-christensen
          DATA_GITHUB_REPO: novel-scenes
          DATA_GITHUB_BRANCH: main
        run: node .github/scripts/daily-email.mjs
